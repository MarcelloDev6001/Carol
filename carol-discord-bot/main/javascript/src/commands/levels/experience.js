const { SlashCommandBuilder } = require("discord.js");
const { BaseInteraction } = require("discord.js");

class SlashCommandsExperience {
  // format:
  // "user_id": {
  //     "xp": 0,
  //     "level": 0
  // }
  static async getXP(interaction, user) {
    // await interaction.reply("Pong!");
  }
}
